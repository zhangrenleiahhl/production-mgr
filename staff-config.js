/* ==========================================================================
   内部人员账号表（发货进度 · 只有内部人员能点四道工序）
   --------------------------------------------------------------------------
   这里存的是口令的 SHA-256（哈希），**不存明文口令**。所以：
   · 忘了口令 → 没法反推出来，只能重新生成一条（用「生成内部口令.html」）
   · 想改口令 → 打开「生成内部口令.html」，工号+姓名+新口令，点生成，
              把给出的那一行替换掉下面 users 里对应的一行，然后重新部署

   ⚠ 安全边界（必须知道）：
     这是「挡住司机、挡住误点」这个级别的门槛，不是金融级安全。
     本项目是纯静态页面（托管在 GitHub Pages），没有服务器端校验，
     所以拿到源码的人理论上可以离线暴力破解口令。
     要做到真正安全，需要「自有备案域名 + 服务器端校验」或「微信登录」。
     因此：**不要把重要系统的口令设成一样**，也不要用弱口令。

   字段说明：
     id    工号（登录时输入它；也可以用工号或姓名登录）
     name  姓名（页面上显示"谁点的这一步"）
     h     口令哈希 = sha256(salt + ":" + 口令)
   days：登录后本机记住多少天，超过就要重新登录
   --------------------------------------------------------------------------
   ⚠ 这里**绝对不能写明文口令**（连注释里写"初始口令 xxx"都不行）：
     这个文件是公开部署的，谁都能下载来看。写一句注释，哈希就白做了。
     初始口令由管理员单独口头/微信告知，忘了就用「生成内部口令.html」重新生成一条。
   ========================================================================== */
window.STAFF = {
  salt: "hg-fhjd-2026-09",
  days: 30,
  users: [
    { id: "1001", name: "管理员", h: "aaefc7797de6f4864744e2d4ad8595b507121bab76ebec0e3487b9172639eb56" },
    { id: "2001", name: "发货员", h: "dc2802a9192802b74de92faab0231762657724ff2579de1412cc3ddd93205dd3" },
    { id: "3001", name: "仓管",   h: "588e420d5dc34566e4656534aeffb5ac8dbeea86a8f490d7cfdfa04beb3b22bc" }
  ]
};
